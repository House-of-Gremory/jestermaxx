export default function Home() {
  // The chat itself lives in the root layout so it remains available around
  // every route. This page is intentionally empty: it prevents the default
  // starter screen from appearing underneath the plain chat controls.
  return null;
}
